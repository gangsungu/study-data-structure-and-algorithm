# study-data-structure-and-algorithm

인프런 온라인 강의([[CS 기술면접 5] 말이 트이는 자료구조](https://www.inflearn.com/course/%EB%B9%84%EC%A0%84%EA%B3%B5%EC%9E%90%EA%B0%80-%EB%86%93%EC%B9%98%EA%B8%B0-%EC%89%AC%EC%9A%B4-%EC%9E%90%EB%A3%8C%EA%B5%AC%EC%A1%B0/dashboard?cid=337909))자료구조와 알고리즘을 정리하는 학습 레포입니다.
정리 문서 · 실행 예제를 **주제 단위로 묶어** 관리하며, 코드는 **Java 기준**으로 작성합니다.

## 목차

읽는 순서대로 정리했습니다. 번호는 강의 회차가 아니라 **정리한 순서**입니다.

| # | 주제 | 다루는 내용 | 예제 |
|:-:|------|------------|:----:|
| 01 | [자료구조 개요](#01-자료구조-개요) | 선형·비선형 분류, 빅-오 표기법, 시공간 트레이드 오프 (문서 2개) | – |
| 02 | [배열과 리스트](02-배열과-리스트/배열과-리스트.md) | 고정 크기와 동적 크기, `ArrayList` vs `LinkedList`, 같은 O(n)이 갈리는 이유 | – |
| 03 | [스택 · 큐 · 덱](03-스택-큐-덱/스택-큐-덱.md) | LIFO·FIFO·양방향, `ArrayDeque`와 원형 버퍼, 연산별 시간 복잡도, 회문 검사 | – |
| 04 | [해시 테이블](04-해시-테이블/해시-테이블.md) | 해시 함수와 `hashCode`·`equals` 계약, 충돌 해결(체이닝·개방 주소법), 적재율과 리사이징, `HashMap` 계열 비교 | – |
| 05 | [그래프](#05-그래프) | 정점·간선과 표현 방식, BFS·DFS, 최단 경로·사이클 판별 (문서 3개) | – |

### 01. 자료구조 개요

**무엇이 있는지**와 **무엇으로 비교하는지**를 나눠 정리했습니다. 위에서부터 읽는 순서입니다.

| 문서 | 다루는 내용 |
|------|------------|
| [자료구조란](01-자료구조-개요/자료구조란.md) | 선형 자료구조(배열·리스트·스택·큐·덱), 비선형 자료구조(해시 테이블·그래프·트리·힙·집합), 추상 자료형과 구현, 자료구조별 자바 구현 한눈에 보기 |
| [시간복잡도와 공간복잡도](01-자료구조-개요/시간복잡도와-공간복잡도.md) | 빅-오 표기법 7가지, 복잡도 구하는 규칙, 최선·평균·최악과 O·Ω·Θ, 분할 상환 분석, 입력 크기로 복잡도 역산하기, 보조 공간과 재귀 스택, 시공간 트레이드 오프 |

> 자료구조 선택은 취향이 아니라 **비용 비교**입니다. 그 비용을 재는 자가 복잡도입니다.

### 05. 그래프

**어떻게 담는지 → 어떻게 도는지 → 무엇을 푸는지** 순서로 나눴습니다.

| 문서 | 다루는 내용 |
|------|------------|
| [그래프](05-그래프/그래프.md) | 정점·간선과 그래프 용어, 방향·가중·DAG 등 종류, 트리와의 관계, 인접 행렬 vs 인접 리스트 vs 간선 리스트, 그래프 알고리즘 지도 |
| [그래프의 탐색](05-그래프/그래프의-탐색.md) | BFS(큐)와 DFS(재귀·스택) 구현, 방문 처리 시점, DFS·BFS 비교, 재귀 깊이와 `StackOverflowError`, 비연결 그래프 |
| [탐색의 활용](05-그래프/탐색의-활용.md) | 비가중 최단 경로와 경로 복원, 연결 요소 세기, 2차원 격자 탐색, 사이클 판별(방향·무방향), 이분 그래프 |

> 탐색 코드의 골격은 하나입니다. **`visited` 자리에 무엇을 기록하느냐**가 문제를 가릅니다.

## 폴더 구조 규칙

새 주제는 **폴더 하나 추가**로 끝납니다.

```
NN-주제이름/
├── 주제이름.md      ← 정리 본문 (주제가 넓으면 여러 문서로 나눈다)
└── examples/       ← 실행 가능한 예제
    ├── 예제-안내.md   ← 예제 목록·실행 방법
    └── ExNN_....java
```

- **번호는 재사용하지도, 다시 매기지도 않는다.** 중간에 배운 주제가 생겨도 맨 뒤에 새 번호로 붙이고, 읽는 순서는 위 목차에서만 조정한다. 그래야 폴더 경로와 링크가 영원히 안 깨진다.
- **`README.md`라는 이름은 레포 루트에만 쓴다.** 파일 이름만 보고 무슨 문서인지 알 수 있어야 한다. 주제마다 `README.md`가 생기면 에디터 탭에서 구분이 안 된다.
- **주제가 넓으면 문서를 나눈다.** 폴더 안에 목차 문서를 따로 만들지 말고 **루트 목차에 하위 표로 나열하고**, 문서끼리는 각 문서 끝의 `### 관련 문서`로 연결한다.
- **파일명에 공백을 쓰지 않는다.** 마크다운 링크가 `%20`으로 지저분해지고 터미널에서 매번 따옴표를 씌워야 한다.
- 예제 파일 이름은 **정리 문서의 섹션 순서와 맞춘다.**

## 예제 실행

Java 11+ 부터는 컴파일 없이 `.java` 파일을 바로 실행할 수 있습니다. **레포 루트에서** 실행하세요.

```bash
java NN-주제이름/examples/ExNN_....java
```

아직 예제를 붙이지 않은 주제는 목차의 예제 열이 `–` 입니다.

> Windows 터미널에서 한글이 깨지면 `java -Dstdout.encoding=UTF-8 ...` 을 쓰거나 `chcp 65001` 을 먼저 실행하세요.
